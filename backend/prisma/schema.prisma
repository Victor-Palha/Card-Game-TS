generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
//create Models yarn prisma migrate dev
model User{
  id String @id @default(uuid())
  email String
  username String
  password String
  admin Boolean @default(false)
  id_solicitante Friends[] @relation("solicitante")
  id_solicitado Friends[] @relation("solicitado")
  id_deck Deck[]

  @@map("users")
}

model Friends{
  id_friend Int @id
  situation Boolean @default(false)
  id_solicitante String
  id_solicitado String
  solicitante User @relation("solicitante", fields: [id_solicitante], references: [id])
  solicitado User @relation("solicitado", fields: [id_solicitado], references: [id])

  @@map("friends")

}

model Deck{
  id_deck String @id
  id_mongo String
  name String

  deck User @relation(fields: [id_deck], references: [id])
  @@map("decks")

}
